.spacer
.align-center
  .content.bg-white.box-center.box-shadow.align-left
    .post-author.bg-grey.z-index
      a(href='/#{item.author.handle}')
        img(src='#{item.author.image}').icon 
        .name.white #{item.author.name}
        .bio.white #{item.author.description}

    .post-title.text-med #{item.title}
    .post-stats.grey#rating-post(data-id= '#{item.id}')
      img(src='/images/like.png') 
      span
        | likes
    br
    .post-content #{item.content}
    .spacer
    include ./commentbox      